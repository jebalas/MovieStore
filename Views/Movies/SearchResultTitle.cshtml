@model IEnumerable<MovieLab24.Data.Movies>

@{
    ViewData["Title"] = "SearchResultTitle";
}

<h1>Movie Search Results for '@ViewBag.TitleSearched'</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<div>
    <a asp-action="Index">Back to Menu</a> |
    <a asp-action="CheckOut">Checkout</a> |
    <a asp-action="ListMovies">Movie List</a>
</div>
